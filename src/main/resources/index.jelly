<!--
  This view is used to render the plugin list page.

  Since we don't really have anything dynamic here, let's just use static HTML. 
-->
<div>
  A plug-in that enables you to instantly run a job on a busy Jenkins cluster (top of the queue, can kill a build to execute now)
</div>